#define _CRT_SECURE_NO_WARNINGS	//解决scanf编译报错
#include <stdio.h>

int main()
{
	int y;
	scanf("%d", &y);
	if (y % 400 == 0 || y % 4 == 0 && y % 100 != 0)   //if判断
	{
		printf("yes\n");
	}
	else {
		printf("no\n");
	}
} 
